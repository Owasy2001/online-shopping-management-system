@model Zaptech.Models.Coupon

@{
    ViewBag.Title = "Create Coupon";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Create Coupon</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="mb-3">
        @Html.LabelFor(m => m.Code)
        @Html.TextBoxFor(m => m.Code, new { @class = "form-control", placeholder = "Enter Coupon Code (e.g., SAVE10)" })
        @Html.ValidationMessageFor(m => m.Code, "", new { @class = "text-danger" })
    </div>

    <div class="mb-3">
        @Html.LabelFor(m => m.DiscountType)
        @Html.DropDownListFor(m => m.DiscountType, new SelectList(new[] { "Percentage", "FixedAmount" }), "-- Select Discount Type --", new { @class = "form-select" })
        @Html.ValidationMessageFor(m => m.DiscountType, "", new { @class = "text-danger" })
    </div>

    <div class="mb-3">
        @Html.LabelFor(m => m.DiscountValue)
        @Html.TextBoxFor(m => m.DiscountValue, new { @class = "form-control", type = "number", step = "0.01", placeholder = "e.g., 10 or 5.99" })
        @Html.ValidationMessageFor(m => m.DiscountValue, "", new { @class = "text-danger" })
    </div>

    <div class="mb-3">
        @Html.LabelFor(m => m.MinimumOrderAmount)
        @Html.TextBoxFor(m => m.MinimumOrderAmount, new { @class = "form-control", type = "number", step = "0.01", placeholder = "Optional (e.g., 100)" })
        @Html.ValidationMessageFor(m => m.MinimumOrderAmount, "", new { @class = "text-danger" })
    </div>

    <div class="row">
        <div class="col-md-6 mb-3">
            @Html.LabelFor(m => m.StartDate)
            @Html.TextBoxFor(m => m.StartDate, "{0:yyyy-MM-dd}", new { @class = "form-control", type = "date" })
            @Html.ValidationMessageFor(m => m.StartDate, "", new { @class = "text-danger" })
        </div>
        <div class="col-md-6 mb-3">
            @Html.LabelFor(m => m.EndDate)
            @Html.TextBoxFor(m => m.EndDate, "{0:yyyy-MM-dd}", new { @class = "form-control", type = "date" })
            @Html.ValidationMessageFor(m => m.EndDate, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="mb-3">
        @Html.LabelFor(m => m.MaxUses)
        @Html.TextBoxFor(m => m.MaxUses, new { @class = "form-control", type = "number", placeholder = "Optional (e.g., 50)" })
        @Html.ValidationMessageFor(m => m.MaxUses, "", new { @class = "text-danger" })
    </div>

    <div class="form-check mb-3">
        @Html.CheckBoxFor(m => m.IsActive, new { @class = "form-check-input" })
        @Html.LabelFor(m => m.IsActive, new { @class = "form-check-label" })
    </div>

    <button type="submit" class="btn btn-primary">Create Coupon</button>
    <a href="@Url.Action("Index")" class="btn btn-secondary">Cancel</a>
}
